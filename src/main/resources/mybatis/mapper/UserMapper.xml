<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bium.chaeum.infrastructure.mybatis.mapper.UserMapper">

    <resultMap id="UserMap" type="UserRecord">
        <id property="userId" column="USER_ID" />
        <result property="email" column="EMAIL" />
        <result property="password" column="PASSWORD" />
        <result property="name" column="NAME" />
    </resultMap>

    <select id="selectById" parameterType="string" resultMap="UserMap">
        SELECT USER_ID, EMAIL, PASSWORD, NAME
        FROM USERS
        WHERE USER_ID = #{userId}
    </select>

    <select id="selectByEmail" parameterType="string" resultMap="UserMap">
        SELECT USER_ID, EMAIL, PASSWORD, NAME
        FROM USERS
        WHERE EMAIL = #{email}
    </select>

    <select id="existsByEmail" parameterType="string" resultType="long">
        SELECT COUNT(*)
        FROM USERS
        WHERE EMAIL = #{email}
    </select>

    <insert id="insert" parameterType="UserRecord">
        INSERT INTO USERS (
            USER_ID,
            EMAIL,
            PASSWORD,
            NAME
        )
        VALUES (
            #{userId},
            #{email},
            #{password},
            #{name}
        )
    </insert>

    <update id="update" parameterType="UserRecord">
        UPDATE USERS SET
            EMAIL = #{email},
            NAME = #{name}
        WHERE
            USER_ID = #{userId}
    </update>
</mapper>